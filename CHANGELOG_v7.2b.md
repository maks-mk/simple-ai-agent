# Changelog v7.2b

## 🎨 UI & User Experience
*   **Centralized Theming**: Внедрен модуль `core/ui_theme.py`, который централизует управление стилями `rich`. Теперь используются семантические имена (например, `agent.thought`, `tool.name`) вместо хардкодных цветов.
*   **Dashboard View**: В `agent_cli.py` реализован табличный режим отображения (`Table.grid`) для "мыслей" агента. Это устраняет скачки текста и улучшает читаемость в реальном времени.
*   **Improved Animations**: Обновлены спиннеры и анимации загрузки для более плавной работы.

## 📝 Logging & Debugging
*   **Logging Refactoring**: Вся конфигурация логирования вынесена в `core/logging_config.py`.
*   **Noise Reduction**: Реализована жесткая фильтрация "шумных" библиотек (httpcore, google.genai и др.).
*   **Split Levels**: 
    *   **Console**: По умолчанию показывает только `WARNING/ERROR` (если `DEBUG=False`), сохраняя чистоту интерфейса.
    *   **File**: В `logs/agent.log` всегда пишутся подробные логи уровня `DEBUG` для пост-анализа.

## 💰 Token Management
*   **Total Token Budget**: Логика бюджета (`core/nodes.py`) теперь учитывает сумму **Input + Output** токенов, а не только входящих.
*   **Fallback Estimation**: `TokenTracker` научился оценивать количество токенов (через `tiktoken` или эвристику), если провайдер API не возвращает метаданные использования (актуально для некоторых прокси/моделей).
*   **Visual Feedback**: В UI добавлена индикация источника токенов (Provider vs Manual/Est).

## 🔧 Internal Improvements
*   **Version Bump**: Проект обновлен до версии **v7.2b**.
*   **Import Fixes**: Исправлены циклические импорты и ошибки инициализации логгера при старте CLI.
