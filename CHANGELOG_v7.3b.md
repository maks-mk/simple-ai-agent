# Changelog v7.3b

## ⚡ Architecture & Core
*   **Simplified Workflow**: Удалены излишние узлы `ToolFilter`, `TokenBudgetGuard` и `SafetyGuard`. Агент теперь работает по прямой линейной схеме с циклами (`Start -> Summarize -> Agent <-> Tools`), полагаясь на нативные возможности LLM для принятия решений.
*   **Removed Over-Engineering**: Убрана сложная логика "фаз" (Exploration/Action) и эвристические проверки намерений. Это снижает задержку и делает поведение агента более предсказуемым.
*   **Native Self-Correction**: Улучшены сообщения об ошибках инструментов. Теперь они возвращают четкие подсказки (`System Hint`), что позволяет модели самостоятельно исправлять аргументы без жестких программных перехватов.

## 🛡️ Stability & Resilience
*   **Ctrl+C Auto-Recovery**: Исправлена критическая ошибка `400 Bad Request` при принудительной остановке генерации. CLI теперь автоматически обнаруживает прерванные вызовы инструментов и заполняет историю заглушками ("Execution interrupted"), позволяя продолжить сессию без перезапуска.
*   **OpenAI Stream Fix**: Устранен конфликт параметров `stream_options` и `include_usage` для OpenAI-совместимых провайдеров (DeepSeek, OpenRouter).

## 🔧 Internal Improvements
*   **Tool Sanitizer**: Сохранена и оптимизирована защита от Path Traversal и инъекций в аргументах инструментов.
*   **Dependency Cleanup**: Удалены неиспользуемые зависимости, связанные со старой логикой гардов.
*   **UI Fixes**: Решены проблемы с отображением в Rich CLI:
    *   Исправлен разрыв строк (теперь выводятся только полные строки).
    *   Восстановлена подсветка синтаксиса кода (заменена тема `ansi_dark` на `dracula`, упрощен regex для извлечения блоков кода).
*   **TokenBudget Removal**: Полностью удалена логика `token_budget` и все упоминания о ней из конфигурации, кода и документации.
