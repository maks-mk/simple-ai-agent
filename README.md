# 🤖 Autonomous AI Agent (LangGraph + MCP + Rich UI)

Продвинутый автономный **AI-агент** на базе **LangGraph** и **Model Context Protocol (MCP)**.
Агент спроектирован для выполнения сложных многошаговых задач с использованием инструментов (Поиск, Файловая система, Память), обладая при этом механизмами самокоррекции и устойчивостью к ошибкам.

Работает в красивом **CLI** интерфейсе с детальной телеметрией.

---

## ✨ Ключевые возможности

### 🛡️ Надежность и Автономность (v3.4)
- **Auto-Retry & Self-Correction:** Агент автоматически перезапускает запросы к LLM при получении битого JSON или пустых ответов.
- **Argument Sanitization:** Встроенный "санитайзер" перехватывает и исправляет ошибки модели в аргументах инструментов (например, удаляет галлюцинированные префиксы `:ru:` из путей файлов или двоеточия из URL).
- **Loop Guard:** Жесткий лимит шагов (`MAX_LOOPS`) предотвращает бесконечные циклы и перерасход бюджета.

### 🧠 Интеллект
- **Dual-Language Reasoning:** Агент "думает" и планирует действия на английском (для максимального качества логики SOTA-моделей), но отвечает пользователю строго на русском.
- **CoT (Chain of Thought):** Отображение скрытых мыслей агента в реальном времени.
- **Context Summarization:** При переполнении контекста старые сообщения сжимаются в саммари, сохраняя важные факты, но освобождая токены.

### 🔌 Инструменты
- **Web Search & Scraping:** Встроенный клиент DuckDuckGo и умный WebFetcher с обходом защиты от ботов и очисткой HTML.
- **File System:** Безопасные операции с файлами (чтение, запись, листинг) внутри рабочей директории.
- **Long-Term Memory:** Векторная память (ChromaDB) для запоминания фактов между сессиями.
- **MCP Support:** Подключение внешних инструментов через стандарт Model Context Protocol.

### 💻 Продвинутый CLI (UI/UX)
- **Точный трекинг токенов:** Суммирует Input/Output токены за все шаги агента, используя официальную статистику API (даже при стриминге).
- **Checklist UI:** Инструменты отображаются в виде списка выполненных задач со статусами (✅/❌) и краткими итогами (например, `Loaded 8013 chars`).
- **Стриминг:** Плавный вывод текста с поддержкой Markdown и анимацией спиннеров.

---

## 🚀 Установка

### 1. Подготовка окружения

Требуется **Python 3.10+**.

```bash
# Клонирование (если есть репозиторий)
# git clone ...

# Создание venv
python -m venv venv

# Активация:
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```

### 2. Зависимости

```bash
pip install -r requirements.txt
```
*Для точного подсчета токенов в режиме эвристики рекомендуется установить `tiktoken`.*

### 3. Настройка `.env`

Создайте файл `.env` на основе примера:

```ini
# --- ВЫБОР ПРОВАЙДЕРА ---
# provider: gemini | openai
PROVIDER=openai

# --- API KEYS ---
# Для Gemini
GEMINI_API_KEY=AIzaSy...
GEMINI_MODEL=gemini-1.5-flash

# Для OpenAI или совместимых (OpenRouter, DeepSeek, LocalLLM)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
# Опционально: свой Base URL
# OPENAI_BASE_URL=https://openrouter.ai/api/v1

# --- НАСТРОЙКИ АГЕНТА ---
# Максимальное кол-во шагов (поиск -> чтение -> ответ = 3 шага)
MAX_LOOPS=25

# Температура (креативность)
LLM_TEMPERATURE=0.2

# Память (RAG)
LONG_TERM_MEMORY=false

# Размер сессии до сжатия (кол-во сообщений)
SESSION_SIZE=20
```

---

## 🏃‍♂️ Запуск

Запустите CLI интерфейс:

```bash
python agent_cli.py
```

**Команды внутри чата:**
- `exit` / `quit` — Выход.
- `reset` / `clear` — Начать новую сессию (сбросить контекст).
- `Alt + Enter` — Перенос строки.

---

## 📁 Структура проекта

| Файл | Описание |
| :--- | :--- |
| `agent.py` | **Ядро.** Логика графа (LangGraph), обработка ошибок, retry-механизмы, санитайзинг аргументов. |
| `agent_cli.py` | **UI.** Асинхронный интерфейс на `rich`. Отвечает за рендеринг, спиннеры и точный подсчет токенов через `stream_events`. |
| `search_tools.py` | Инструменты поиска (DuckDuckGo) и парсинга веб-страниц (WebFetcher). |
| `delete_tools.py` | Безопасные инструменты для файловой системы. |
| `memory_manager.py` | Управление долговременной памятью (ChromaDB). |
| `logging_config.py` | Настройка тихого логирования (скрывает шум библиотек). |
| `prompt.txt` | Системный промпт с инструкциями. |
| `mcp.json` | Конфиг для подключения MCP-серверов. |