# 🤖 Autonomous AI Agent (LangGraph + MCP + Rich UI)

**v6.0 (Stable & Modular)**

Продвинутый автономный **AI-агент** на базе **LangGraph** и **Model Context Protocol (MCP)**.
Агент построен на принципах модульной архитектуры: логика выполнения, безопасность, очистка данных, валидация и UI разделены на независимые компоненты.

Обладает **гибридным интеллектом**: умеет отличать творческие задачи от фактологических, защищая пользователя от галлюцинаций, и диагностировать систему.

---

## ✨ Ключевые возможности

### 🛡️ Интеллектуальная Безопасность (SafetyGuard)
- **Dynamic Quality Gate:** Эвристическая защита от записи выдуманных данных. Агент обязан найти информацию перед записью в файл, если задача не является творческой.
- **Loop Guard:** Жесткая защита от зацикливания.
- **Self-Healing:** Агент автоматически перехватывает ошибки инструментов (например, `ENOENT` или `401`), анализирует их и пытается исправить параметры запроса без падения программы.

### 🔌 Инструменты и MCP
- **MCP Filesystem:** Работа с файловой системой (чтение, запись, редактирование, листинг) реализована через стандартный протокол **MCP** (`mcp-server-filesystem`).
- **System Tools:** Нативные инструменты для диагностики: определение публичного IP, геолокации, характеристик "железа" (CPU/RAM/Disk) и локальной сети.
- **Smart Search:** Умный поиск (`batch_web_search`) и глубокое чтение контента (`fetch_content`).
- **Long-Term Memory:** Векторная память (ChromaDB) для запоминания фактов между сессиями.

### 💻 Продвинутый CLI (UI/UX)
- **Real-time Streaming:** Посимвольный вывод ответа.
- **System Diagnostics:** Агент может проверить доступность сети и состояние сервера.
- **Hybrid Token Tracking:** Поддержка точного подсчета токенов от провайдера или эвристический подсчет (`Manual`) для совместимости с любыми моделями.

---

## 🚀 Установка

### 1. Подготовка окружения

Требуется **Python 3.10+** и **Node.js** (для работы MCP серверов).

```bash
# Создание venv
python -m venv venv

# Активация (Windows):
venv\Scripts\activate
# Активация (Linux/Mac):
source venv/bin/activate
```

### 2. Зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройка `.env`

Создайте файл `.env`. Выберите **один** из вариантов настройки провайдера (Gemini или OpenAI).

#### Вариант А: Google Gemini (Рекомендуется для старта)
```ini
# --- ВЫБОР ПРОВАЙДЕРА ---
PROVIDER=gemini

# --- GEMINI SETTINGS ---
GEMINI_API_KEY=AIzaSy...
GEMINI_MODEL=gemini-1.5-flash
# Или более мощная: gemini-1.5-pro

# --- ОБЩИЕ НАСТРОЙКИ ---
TAVILY_API_KEY=tvly-...
MAX_LOOPS=15
TEMPERATURE=0.2
LONG_TERM_MEMORY=true
```

#### Вариант Б: OpenAI / DeepSeek / OpenRouter
```ini
# --- ВЫБОР ПРОВАЙДЕРА ---
PROVIDER=openai

# --- OPENAI SETTINGS ---
OPENAI_API_KEY=sk-...
# Для OpenRouter/DeepSeek измените base_url:
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# --- ОБЩИЕ НАСТРОЙКИ ---
TAVILY_API_KEY=tvly-...
MAX_LOOPS=15
TEMPERATURE=0.2
LONG_TERM_MEMORY=true
```

### 4. Настройка MCP (`mcp.json`)

Убедитесь, что у вас установлен сервер файловой системы (требуется npx):

```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
    "transport": "stdio",
    "enabled": true
  }
}
```

---

## 🏃‍♂️ Запуск

```bash
python agent_cli.py
```

**Команды внутри чата:**
- `exit` / `quit` — Выход.
- `reset` / `clear` — Сброс контекста (новая сессия).
- `Alt + Enter` — Перенос строки.

---

## 📁 Структура проекта

### Корневые файлы
| Файл | Назначение |
| :--- | :--- |
| `agent.py` | **Core Flow.** Граф выполнения, встроенный валидатор, управление состоянием. |
| `agent_cli.py` | **UI.** Точка входа, обработка ввода/вывода, спиннеры. |
| `mcp.json` | Конфигурация внешних инструментов (Filesystem). |

### Модули `core/` (Ядро)
| Файл | Назначение |
| :--- | :--- |
| `config.py` | Настройки (Pydantic), выбор LLM. |
| `safety_guard.py` | Политики безопасности (Quality Gate). |
| `tool_validator.py` | Логика проверки ошибок (анализ ответов инструментов). |
| `tool_sanitizer.py` | Очистка аргументов и путей. |
| `utils.py`, `cli_utils.py` | Вспомогательные функции, трекер токенов. |

### Модули `tools/` (Инструменты)
| Файл | Назначение |
| :--- | :--- |
| `tool_registry.py` | Загрузчик всех инструментов (локальных и MCP). |
| `search_tools.py` | Поиск в интернете (Tavily). |
| `system_tools.py` | Диагностика системы (IP, CPU, RAM, Network). |
| `patch_tool.py` | `smart_replace` для надежного редактирования кода. |
| `delete_tools.py` | Безопасное удаление файлов. |
| `memory_manager.py` | Векторная память (ChromaDB). |

---

## 🛠 Граф выполнения

```mermaid
graph TD
    START --> Summarize
    Summarize --> Agent
    Agent -- Tool Call --> ToolsAndValidate
    ToolsAndValidate -- Execution & Validation --> Agent
    Agent -- Response --> END
    Agent -- Loop Limit --> LoopGuard --> END
