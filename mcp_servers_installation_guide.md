# 🛠️ Полное руководство по MCP серверам (Windows)

В этом руководстве описаны два способа подключения инструментов:
1.  **Без установки (On-the-fly):** Серверы скачиваются и запускаются автоматически. Идеально для переносимости.
2.  **Глобальная установка:** Серверы устанавливаются в систему один раз. Идеально для работы без интернета и мгновенного запуска.

---

## ✅ Предварительные требования

Перед началом убедитесь, что у вас установлены базовые инструменты.

### 1. Node.js (для JS серверов)
Скачайте и установите **Node.js (LTS)**: [nodejs.org](https://nodejs.org/)
Проверка:
```powershell
node -v
npm -v
```

### 2. UV (для Python серверов)
Лучший менеджер пакетов для Python (быстрее pip).
В PowerShell (от Администратора):
```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```
Проверка:
```powershell
uv --version
```

---

## 🚀 Способ 1: Запуск "На лету" (Рекомендуемый)

**Суть:** Вам не нужно ничего устанавливать вручную. Вы прописываете команды `uvx` и `npx` в конфиг, и агент сам скачивает нужные инструменты во временную среду при запуске.

**Плюсы:**
*   Не засоряет систему.
*   Всегда свежие версии.
*   Код будет работать на другом компьютере без настройки.

**Минусы:**
*   Первый запуск чуть медленнее (скачивание).

### Конфигурация `mcp.json` для способа 1

```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
    "transport": "stdio",
    "enabled": true
  },
  "duckduckgo": {
    "command": "uvx",
    "args": ["duckduckgo-mcp-server"],
    "transport": "stdio",
    "enabled": true
  },
  "fetch": {
    "command": "uvx",
    "args": ["mcp-server-fetch"],
    "transport": "stdio",
    "enabled": true
  },
  "win-cli": {
    "command": "npx",
    "args": ["-y", "@simonb97/server-win-cli", "--config", "win-cli-config.json"],
    "transport": "stdio",
    "enabled": false
  }
}
```

---

## 💾 Способ 2: Глобальная установка (Классический)

**Суть:** Вы устанавливаете пакеты один раз в систему. Агент вызывает их напрямую.

**Плюсы:**
*   Мгновенный старт (не тратится время на разрешение зависимостей).
*   Работает без интернета.

**Минусы:**
*   Нужно следить за обновлениями вручную.
*   При переносе проекта на другой ПК нужно всё устанавливать заново.

### Шаг 1. Установка пакетов

Выполните в терминале:

```bash
# === Node.js серверы ===
npm install -g @modelcontextprotocol/server-filesystem
npm install -g @simonb97/server-win-cli

# === Python серверы (через UV) ===
# uv tool install делает команду доступной глобально
uv tool install duckduckgo-mcp-server
uv tool install mcp-server-fetch
```

### Шаг 2. Конфигурация `mcp.json` для способа 2

**Важно для Windows:** Глобальные NPM пакеты создают `.cmd` файлы. Их нужно указывать явно, иначе Python `subprocess` может их не найти.

```json
{
  "filesystem": {
    "command": "mcp-server-filesystem.cmd",
    "args": ["."],
    "transport": "stdio",
    "enabled": true
  },
  "duckduckgo": {
    "command": "duckduckgo-mcp-server",
    "args": [],
    "transport": "stdio",
    "enabled": true
  },
  "fetch": {
    "command": "mcp-server-fetch",
    "args": [],
    "transport": "stdio",
    "enabled": true
  },
  "win-cli": {
    "command": "mcp-server-win-cli.cmd",
    "args": ["--config", "win-cli-config.json"],
    "transport": "stdio",
    "enabled": false
  }
}
```

> *Если команды типа `mcp-server-filesystem.cmd` не находятся, попробуйте использовать полный путь, который покажет команда `where mcp-server-filesystem`.*

---

## 🛡️ Безопасность (Обязательно для win-cli)

Если вы используете сервер **win-cli** (управление командной строкой), обязательно создайте файл конфигурации рядом с `mcp.json`, чтобы агент не удалил случайно важные файлы.

**Файл:** `win-cli-config.json`

```json
{
  "powershell": {
    "enabled": true,
    "timeout": 30000
  },
  "cmd": {
    "enabled": true,
    "timeout": 30000
  },
  "allowed_commands": [
    "dir", "ls", "echo", "type", "cat", 
    "ipconfig", "whoami", "git", "python", 
    "uv", "npm", "node", "tree", "where"
  ],
  "blocked_commands": [
    "del", "rm", "rmdir", "format", "shutdown", 
    "reboot", "taskkill", "mklink"
  ],
  "working_directory": "."
}
```

---

## ❓ Часто задаваемые вопросы

### Что лучше: `pip install` или `uv tool install`?
Для MCP серверов **всегда используйте `uv tool install`**.
*   `pip install` ставит пакеты в глобальную кучу Python, что может вызвать конфликты версий.
*   `uv tool install` создает изолированную среду для каждого инструмента, но делает его доступным глобально. Это надежнее.

### Ошибка: `spawn ENOENT` или `command not found`
Агент не может найти исполняемый файл.
1.  Убедитесь, что пути добавлены в системную переменную **PATH**.
2.  Перезагрузите компьютер или VS Code.
3.  Если не помогло — используйте **Способ 1** (через `uvx`/`npx`), он самый надежный.

### Ошибка Telegram сервера
Telegram требует `API_ID` и `API_HASH`.
1.  Получите их на [my.telegram.org](https://my.telegram.org).
2.  Не пишите их в `mcp.json` напрямую, если планируете делиться файлом! Лучше использовать переменные окружения, но текущая реализация загрузчика MCP читает их из JSON.
    *   *Совет:* Добавьте `mcp.json` в `.gitignore`, если там есть ключи.