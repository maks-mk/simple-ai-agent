# 📄 PDFMcp — MCP PDF Server

MCP-сервер для работы с PDF-файлами. Позволяет AI-ассистентам (Claude и др.) создавать, редактировать, искать и манипулировать PDF прямо из чата.

---

## ✨ Возможности

| Инструмент | Описание |
|---|---|
| `pdf_create` | Создание PDF из текста с Markdown-форматированием |
| `pdf_extract_text` | Извлечение текста постранично (с пагинацией и сортировкой) |
| `pdf_extract_images` | Извлечение всех изображений в ZIP-архив |
| `pdf_search` | Поиск текста с координатами, контекстом и индексами вхождений |
| `pdf_edit_text` | Замена/удаление текста с точным указанием вхождения |
| `pdf_add_text` | Добавление текста в произвольную позицию на странице |
| `pdf_merge` | Объединение нескольких PDF в один |
| `pdf_split` | Разбивка PDF на части по диапазонам страниц |
| `pdf_rotate_pages` | Поворот страниц на 90 / 180 / 270° |
| `pdf_from_images` | Конвертация изображений (JPG, PNG) в PDF |

---

## 🔧 Требования

- Python **≥ 3.10**
- [`uv`](https://github.com/astral-sh/uv) (рекомендуется) или `pip`

---

## 📦 Установка

### Вариант 1 — через `uv` (рекомендуется)

```bash
# Установить uv, если ещё не установлен
curl -LsSf https://astral.sh/uv/install.sh | sh

# Зависимости установятся автоматически при первом запуске
uv run pdf_server.py
```

### Вариант 2 — через `pip`

```bash
pip install "mcp[cli]" pymupdf reportlab
python pdf_server.py
```

---

## ⚙️ Настройка в Claude Desktop

Откройте файл конфигурации Claude Desktop:

- **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

### С использованием `uv` (рекомендуется)

```json
{
  "mcpServers": {
    "pdfmcp": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp[cli]",
        "--with", "pymupdf",
        "--with", "reportlab",
        "/ПОЛНЫЙ/ПУТЬ/К/pdf_server.py"
      ]
    }
  }
}
```

### С использованием `python`

```json
{
  "mcpServers": {
    "pdfmcp": {
      "command": "python",
      "args": ["/ПОЛНЫЙ/ПУТЬ/К/pdf_server.py"]
    }
  }
}
```

> ⚠️ Укажите **абсолютный путь** к файлу `pdf_server.py`. После сохранения **перезапустите Claude Desktop**.

---

## ⚙️ Настройка в Claude Code (CLI)

```bash
claude mcp add pdfmcp -- uv run --with "mcp[cli]" --with pymupdf --with reportlab /ПОЛНЫЙ/ПУТЬ/К/pdf_server.py
```

---

## ⚙️ Настройка в других MCP-клиентах

```json
{
  "mcpServers": {
    "pdfmcp": {
      "command": "python",
      "args": ["/путь/к/pdf_server.py"],
      "transport": "stdio"
    }
  }
}
```

---

## 🔤 Шрифты и кириллица

Сервер автоматически определяет системные шрифты с поддержкой кириллицы:

**Windows:** Arial → Verdana → Tahoma → Segoe UI  
**Linux:** DejaVu Sans → Liberation Sans → Free Sans  
**macOS:** системный список не включён → используется Roboto (см. ниже)

Если ни один системный шрифт не найден, сервер **автоматически скачает Roboto** из Google Fonts в `/tmp/mcp_fonts_roboto/`. Интернет нужен только при первом запуске.

---

## 📝 Поддерживаемый Markdown в `pdf_create`

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3
#### Заголовок 4

**жирный**, __жирный__
*курсив*
`инлайн код`
[ссылка](https://example.com)

---  (горизонтальная линия)

- пункт списка
* пункт списка

> цитата

| Колонка 1 | Колонка 2 |
|-----------|-----------|
| ячейка    | ячейка    |

блок кода

![подпись](/путь/к/картинке.png)
```

> **Примечание:** если `title` и первая строка `content` совпадают (`# Заголовок`), дубликат автоматически пропускается.

---

## 📋 Описание инструментов

### `pdf_create`
Создаёт PDF-файл из текста с Markdown-разметкой.

| Параметр | Тип | Описание |
|---|---|---|
| `output_path` | `str` | Путь для сохранения файла |
| `title` | `str` | Заголовок документа |
| `content` | `str` | Содержимое в Markdown-формате |

---

### `pdf_extract_text`
Извлекает текст из PDF с разбивкой по страницам. Используйте `page_start`/`page_end` для больших документов — это предотвращает переполнение контекста модели.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `page_start` | `int` | Первая страница (с 1, по умолчанию: `1`) |
| `page_end` | `int?` | Последняя страница включительно (по умолчанию: все). Рекомендуемый чанк: 20–50 страниц |
| `sort` | `bool` | Сортировать текст по координатам (по умолчанию: `true`). Отключите для ускорения, если порядок не важен |

Пример ответа:
```
[PDF: document.pdf | Pages 1-30 of 120]

[Page 1]
Текст первой страницы...
```

---

### `pdf_extract_images`
Извлекает все изображения из PDF и упаковывает в ZIP-архив.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `quality` | `int` | Качество JPEG 1–100 (по умолчанию: `90`) |

---

### `pdf_search`
Ищет текст в PDF. Возвращает страницу, **индекс вхождения**, координаты и контекст. Используйте перед `pdf_edit_text`, чтобы узнать точное количество и позиции вхождений.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `query` | `str` | Текст для поиска |
| `case_sensitive` | `bool` | Учитывать регистр (по умолчанию: `false`) |

Пример ответа:
```
Found 2 occurrence(s):
Page 1 occurrence #1: rect=(50.0, 80.0, 400.0, 104.0) | context: "...Заголовок документа..."
Page 1 occurrence #2: rect=(50.0, 110.0, 400.0, 134.0) | context: "...Заголовок документа..."
```

---

### `pdf_edit_text`
Заменяет или удаляет текст на указанной странице. Поддерживает точное указание вхождения для работы с дубликатами.
**Автоматически оптимизирует размер файла** при сохранении (удаляет неиспользуемые объекты и сжимает потоки).

> ⚠️ **Рекомендуется сначала вызвать `pdf_search`**, чтобы убедиться в количестве вхождений и выбрать нужный `occurrence`.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `page_num` | `int` | Номер страницы (с 1) |
| `old_text` | `str` | Точный текст для замены (регистрозависимо) |
| `new_text` | `str` | Новый текст. Пустая строка `""` — удалить без замены |
| `output_path` | `str?` | Путь для сохранения (по умолчанию: `*_edited.pdf`) |
| `occurrence` | `int` | Какое вхождение заменить: `0` = все (по умолчанию), `1` = первое, `2` = второе и т.д. |

**Типичный сценарий — удалить дубликат заголовка:**
```
# 1. Проверяем вхождения
pdf_search(pdf_path="doc.pdf", query="Заголовок")
→ Found 2 occurrence(s): Page 1 occurrence #1 ... Page 1 occurrence #2 ...

# 2. Удаляем только второе вхождение
pdf_edit_text(pdf_path="doc.pdf", page_num=1, old_text="Заголовок", new_text="", occurrence=2)
→ Saved to doc_edited.pdf (deleted occurrence #2, logical occurrences on page: 2)
```

---

### `pdf_add_text`
Добавляет новый текст на указанную страницу в заданные координаты.
**Автоматически оптимизирует размер файла** при сохранении.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `page_num` | `int` | Номер страницы (с 1) |
| `text` | `str` | Текст для добавления |
| `x` | `float` | Координата X (отступ слева, по умолчанию: `56.0`) |
| `y` | `float?` | Координата Y (отступ сверху). Если не указано, текст добавится после последнего блока текста |
| `fontsize` | `float` | Размер шрифта (по умолчанию: `10.0`) |
| `output_path` | `str?` | Путь для сохранения (по умолчанию: `*_edited.pdf`) |

---

### `pdf_merge`
Объединяет несколько PDF в один файл.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_paths` | `list[str]` | Список путей к PDF (в нужном порядке) |
| `output_path` | `str` | Путь для сохранения результата |

---

### `pdf_split`
Разбивает PDF на части по диапазонам страниц.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `ranges` | `list[str]` | Диапазоны страниц, например `["1-3", "5", "7-9"]` |
| `output_dir` | `str` | Директория для сохранения частей |

---

### `pdf_rotate_pages`
Поворачивает страницы PDF.

| Параметр | Тип | Описание |
|---|---|---|
| `pdf_path` | `str` | Путь к PDF-файлу |
| `rotation` | `int` | Угол поворота: `90`, `180`, `270` |
| `pages` | `list[int]?` | Список страниц (с 1). Если не указан — все страницы |

---

### `pdf_from_images`
Конвертирует список изображений в один PDF.

| Параметр | Тип | Описание |
|---|---|---|
| `image_paths` | `list[str]` | Пути к изображениям (JPG, PNG) |
| `output_path` | `str` | Путь для сохранения PDF |

---

## ⚠️ Ограничения

- Максимальный размер PDF: **100 MB**
- Поддерживаются только файлы с расширением `.pdf`
- `pdf_edit_text` использует redaction — стандартный подход всех PDF-редакторов (включая Adobe Acrobat). PDF не поддерживает редактирование текста «на месте»: текст хранится как поток команд рендеринга, а не как структурированный контент. Инструмент закрашивает область, определяет размер и цвет оригинального текста и вставляет новый текст с теми же параметрами. Шрифт подбирается из системных (не из embedded PDF-шрифтов)
- Для корректного отображения кириллицы нужен один из поддерживаемых системных шрифтов или доступ в интернет для загрузки Roboto

---

## 🛠️ Устранение проблем

**Кириллица отображается квадратиками**  
→ Убедитесь, что на машине установлен один из поддерживаемых шрифтов, или разрешите доступ в интернет для загрузки Roboto.

**Сервер не появляется в Claude**  
→ Проверьте абсолютный путь в конфиге, убедитесь что JSON валиден, перезапустите Claude Desktop.

**`uv: command not found`**  
→ Установите uv: `curl -LsSf https://astral.sh/uv/install.sh | sh` или используйте вариант с `python`.

**Ошибка при установке через pip**  
→ Попробуйте `pip install --break-system-packages "mcp[cli]" pymupdf reportlab` или используйте виртуальное окружение.

**`pdf_edit_text` удаляет все вхождения вместо одного**  
→ Сначала вызовите `pdf_search`, чтобы увидеть индексы вхождений, затем передайте нужный `occurrence=N`.

**Текст после редактирования не виден / обрезан**  
→ В ответе будет предупреждение `Warning: text may be truncated`. Используйте более короткий текст замены.

---

## 📄 Лицензия

MIT
